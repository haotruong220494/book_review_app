<div class="col-12 pt-3">
  <div class="card">
    <div class="card-header">
      <h3 class="card-title"><i class="fas fa-list"></i>&ensp;Post list</h3>
      <%= link_to new_admin_post_path, class: "create-category" do %>
        <i class="far fa-plus-square nav-icon"></i>
      <% end %>
      <div class="card-tools">
        <%= search_form_for @q, url: admin_posts_path do |f| %>
          <div class="input-group input-group-sm">
            <%= f.search_field :name_cont, class: "form-control float-right", placeholder: "Search article... " %>
            <div class="input-group-append">
              <%= f.submit "search", class: "button btn btn-default" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body table-responsive p-0">
      <table class="table table-hover text-nowrap">
        <thead>
          <tr>
            <th scope="col">Title</th>
            <th scope="col">Category</th>
            <th scope="col">Content</th>
            <th scope="col">Slug</th>
            <th colspan="2"></th>
          </tr>
        </thead>
        <tbody>
          <%= render @posts %>
        </tbody>
      </table>
    </div>
  </div>
  <%= paginate @posts, outer_window: 3 %>
</div>
